# Specs that the React migration is expected to keep green, one filename per line.
# CI (see .github/workflows/playwright.yml and security-tests.yml) runs only these while
# the Angular -> React migration is in flight; each phase appends the specs it takes
# ownership of, and previously listed specs must stay green. The Phase 7 cutover deletes
# this file and CI goes back to running the whole suite.
#
# Phase 0 lists nothing: no page is ported yet.
#
# Phase 2 (auth store, app shell, router skeleton, guards):
health.spec.ts
#
# Phase 4 (home, article and editor pages, article comments): the home feed now exists, so
# the two deferred user-fetch-errors cases (.article-preview / Global Feed / .tag-pill) pass
# and the whole file is green.
user-fetch-errors.spec.ts
articles.spec.ts
comments.spec.ts
#
# Phase 3 (shared article UI: ArticleList / ArticlePreview / ArticleMeta, sanitized markdown)
# adds no spec: xss-security.spec.ts still needs the profile page (.user-info, its article
# feed) and the article page (.article-content), both of which Wave 3 delivers. 2 of its 16
# tests pass today (navbar image, article comments); the other 14 time out waiting for those
# pages. It goes on this list with Phase 4/5/6.
#
# Phase 6 (settings page, login/register with React Hook Form):
auth.spec.ts
# settings.spec.ts stays out until the real profile page lands: 5 of its 7 tests pass, the
# other 2 assert .user-info / .user-img on the profile the settings update redirects to.
# error-handling.spec.ts stays out too — its settings and login/register cases (400/401/500
# and the three network-failure ones) all pass now, but the spec also covers the editor,
# comment and article pages that Wave 3's other phases deliver.
